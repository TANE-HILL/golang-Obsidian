_Практика по итогу раздела_  
*"[[2.1.1. ORM и Query Builders (sqlx, GORM, ent)|ORM и Query Builders (sqlx, GORM, ent)]]"*

Продолжаем развивать проект GoFinance.
В этом задании второй важный модуль — **работа с транзакциями**.

Теперь сервис сможет не только хранить категории (доходы/расходы), но и **записывать реальные операции** — пополнения, траты, переводы.

---
# Цель

Научиться создавать новые таблицы и ручки API, которые выполняют **реальные SQL-запросы** через `sqlx`.

---
# Техническое задание

## Описание

**В проекте GoFinance реализовать новый модуль:**
- таблицу `transactions` в PostgreSQL;
- модель `Transaction` в Go;
- репозиторий `TransactionRepo` с методами `Create` и `List`;
- HTTP-ручки `/transactions` (POST и GET);
- регистрацию маршрута в `main.go`.

Это **простые операции с SQL**, мы пока **не используем BEGIN/COMMIT** — они будут в следующем уроке.

---
## Структура проекта после выполнения

```
gofinance/
├── cmd/api/
│   └── main.go
├── internal/
│   ├── db/
│   │   ├── connect.go
│   │   ├── category_repo.go
│   │   └── transaction_repo.go      ✅ новый файл
│   ├── handlers/
│   │   ├── categories.go
│   │   └── transactions.go          ✅ новый файл
│   └── models/
│       ├── category.go
│       └── transaction.go           ✅ новый файл
└── migrations/
    ├── 001_create_categories.sql
    └── 002_create_transactions.sql  ✅ новый файл

```

---
# Что нужно реализовать

### 1. Таблица `transactions`

Файл: `migrations/002_create_transactions.sql`
Таблица для хранения финансовых операций:
```sql
CREATE TABLE IF NOT EXISTS transactions (
    id SERIAL PRIMARY KEY,
    category_id INT REFERENCES categories(id) ON DELETE CASCADE,
    amount NUMERIC(10,2) NOT NULL,
    note TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);
```
- `category_id` — ссылка на категорию (`income` / `expense`);
- `amount` — сумма (можно положительную или отрицательную);
- `note` — описание, например “обед в кафе”;
- `created_at` — автоматически ставится при добавлении записи.

---
### 2. Модель `Transaction`

Файл: `internal/models/transaction.go`
Структура, которая будет описывать транзакцию:
```go
package models

type Transaction struct {
    ID         int     `db:"id" json:"id"`
    CategoryID int     `db:"category_id" json:"category_id"`
    Amount     float64 `db:"amount" json:"amount"`
    Note       string  `db:"note" json:"note"`
    CreatedAt  string  `db:"created_at" json:"created_at"`
}

```

---
### 3. Репозиторий `TransactionRepo`

Файл: `internal/db/transaction_repo.go`

Добавь методы для добавления и получения транзакций.

Требования:
- Метод `Create()` должен вставлять новую запись в БД и возвращать результат.
- Метод `List()` — выбирать все транзакции, отсортированные по дате.

Подсказка: используй `r.db.GetContext()` и `r.db.SelectContext()` — они делают то же самое, что и `QueryRow`/`Query`, но сразу маппят данные в структуру.

---
### 4. Хендлеры `/transactions`

Файл: `internal/handlers/transactions.go`

Реализуй два хендлера:
1. `POST /transactions` — добавляет транзакцию
	(JSON с `category_id`, `amount`, `note`).
2. `GET /transactions` — возвращает список всех транзакций.

---
### 5. Регистрация маршрута

Файл: `cmd/api/main.go`

Новый блок для `/transactions`:
```go
txs := handlers.NewTransactionHandlers(dbase)

http.HandleFunc("/transactions", func(w http.ResponseWriter, r *http.Request) {
    switch r.Method {
    case http.MethodGet:
        txs.ListTransactions(w, r)
    case http.MethodPost:
        txs.CreateTransaction(w, r)
    default:
        http.Error(w, "Method Not Allowed", http.StatusMethodNotAllowed)
    }
})
```

---
# Проверка работы

### 1. Запустить сервер
```bash
go run ./cmd/api
```

### 2. Добавь транзакцию
```bash
curl -X POST <http://localhost:8080/transactions> \
  -H "Content-Type: application/json" \
  -d '{"category_id":1,"amount":-250.00,"note":"Обед в кафе"}'
```
Получить ответ:
```json
{
  "id": 1,
  "category_id": 1,
  "amount": -250,
  "note": "Обед в кафе",
  "created_at": "2025-10-25T12:00:00Z"
}
```

### 3. Получить список транзакций
```bash
curl <http://localhost:8080/transactions>
```
Получить ответ:
```json
[
  {
    "id": 1,
    "category_id": 1,
    "amount": -250,
    "note": "Обед в кафе",
    "created_at": "2025-10-25T12:00:00Z"
  }
]
```

---
## Критерии готовности

- [ ] Создана таблица `transactions` в БД
- [ ] Добавлены файлы `transaction.go`, `transaction_repo.go`, `transactions.go`
- [ ] Роут `/transactions` зарегистрирован в `main.go`
- [ ] Методы `POST` и `GET` работают корректно
- [ ] Ответы возвращаются в JSON-формате
- [ ] Код структурирован, читаемый, ошибки обрабатываются

---
# Задача со звёздочкой (по желанию)

Добавь фильтрацию транзакций по категории:

```
GET /transactions?category_id=2

```

💡 Подсказка:

- получи параметр `category_id` через `r.URL.Query().Get("category_id")`;
- если он задан — добавь `WHERE category_id = $1` в SQL-запрос;
- иначе возвращай все транзакции.