Работа над проектом по итогу Модуля 6.

#### <u>Структура проекта:</u>
![[Pasted image 20260826022038.png|319]]

#### <u>main.go:</u>
```Go
package main  
  
import (  
    "fmt"  
    "gotracker/internal/order"    "gotracker/internal/repository"    "gotracker/internal/service")  
  
func main() {  
    repo := repository.NewInMemoryOrderRepo()  
    svc := service.NewOrderService(repo)  
    repo.Add(order.Order{ID: 1, Customer: "Андрей", Address: "40 лет победы, 31-Б"})  
    repo.Add(order.Order{ID: 2, Customer: "Иван", Address: "ул. Кирова, 136"})  
    repo.Add(order.Order{ID: 3, Customer: "Анастасия", Address: "ул. Васенко, 15"})  
    repo.Add(order.Order{ID: 4, Customer: "Валерий", Address: "пос. Западный, д. 9"})  
    repo.Add(order.Order{ID: 5, Customer: "Альберт", Address: "ул. Петра Столыпина, д. 112"})  
    repo.Add(order.Order{ID: 6, Customer: "Владислав", Address: "Винегретовая ул., д. 9"})  
    repo.Add(order.Order{ID: 7, Customer: "Даниил", Address: "ул. Карла Маркса, 54"})  
    fmt.Println("[Before]")  
    svc.PrintAllOrders()  
  
    fmt.Println("\n[Delivery Result]")  
    ids := []int{1, 2, 5, 8}  
    svc.DeliverMany(ids)  
  
    fmt.Println("\n[After]")  
    svc.PrintAllOrders()  
}
```

#### <u>order.go:</u>
```Go
package order  
  
import "errors"  
  
var ErrOrderNotFound = errors.New("order not found")  
  
type Order struct {  
    ID          int  
    Customer    string  
    Address     string  
    IsDelivered bool  
}  
  
func (o *Order) MarkDelivered() {  
    o.IsDelivered = true  
}
```

#### <u>inmemory.go:</u>
```Go
package repository  
  
import (  
    "gotracker/internal/order"  
    "sync")  
  
type InMemoryOrderRepo struct {  
    Orders map[int]order.Order  
    mu     sync.Mutex  
}  
  
func NewInMemoryOrderRepo() *InMemoryOrderRepo {  
    return &InMemoryOrderRepo{  
       Orders: make(map[int]order.Order),  
    }  
}  
  
func (repo *InMemoryOrderRepo) Add(order order.Order) {  
    defer repo.mu.Unlock()  
    repo.mu.Lock()  
    repo.Orders[order.ID] = order  
}  
  
func (repo *InMemoryOrderRepo) GetByID(id int) (order.Order, error) {  
    defer repo.mu.Unlock()  
    repo.mu.Lock()  
    value, ok := repo.Orders[id]  
    if !ok {  
       return value, order.ErrOrderNotFound  
    }  
    return value, nil  
}  
  
func (repo *InMemoryOrderRepo) Update(o order.Order) error {  
    defer repo.mu.Unlock()  
    repo.mu.Lock()  
    if _, ok := repo.Orders[o.ID]; !ok {  
       return order.ErrOrderNotFound  
    }  
    repo.Orders[o.ID] = o  
    return nil  
}  
  
func (repo *InMemoryOrderRepo) GetAll() []order.Order {  
    defer repo.mu.Unlock()  
    repo.mu.Lock()  
    orders := make([]order.Order, 0, len(repo.Orders))  
    for _, o := range repo.Orders {  
       orders = append(orders, o)  
    }  
    return orders  
}
```

#### <u>interface.go:</u>
```Go
package repository  
  
import (  
    "gotracker/internal/order"  
)  
  
type Repository interface {  
    Add(order order.Order)  
    GetByID(id int) (order.Order, error)  
    Update(order order.Order) error  
    GetAll() []order.Order  
}
```

#### <u>order_service.go:</u>
```Go
package service  
  
import (  
    "fmt"  
    "gotracker/internal/repository"    "sync")  
  
type OrderService struct {  
    repo repository.Repository  
}  
  
func NewOrderService(repo repository.Repository) *OrderService {  
    return &OrderService{repo: repo}  
}  
  
func (service *OrderService) DeliverMany(ids []int) {  
    var wg sync.WaitGroup  
    for _, id := range ids {  
       wg.Add(1)  
       go func() {  
          defer wg.Done()  
          order, errGet := service.repo.GetByID(id)  
          if errGet != nil {  
             fmt.Printf("Ошибка: %s (ID: %v)\n", errGet, id)  
             return  
          }  
          order.MarkDelivered()  
          if errUpdate := service.repo.Update(order); errUpdate != nil {  
             fmt.Printf("Ошибка: %s (ID: %v)\n", errUpdate, id)  
             return  
          }  
          fmt.Printf("Updated order %v\n", order.ID)  
       }()  
    }  
    wg.Wait()  
}  
func (service *OrderService) PrintAllOrders() {  
    orders := service.repo.GetAll()  
    for _, v := range orders {  
       fmt.Printf("ID: %v, %v, Delivered: %v\n", v.ID, v.Customer, v.IsDelivered)  
    }  
}
```

