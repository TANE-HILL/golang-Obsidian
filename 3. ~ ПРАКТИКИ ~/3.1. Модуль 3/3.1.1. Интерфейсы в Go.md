_Практика по итогу модуля_  
*"[[1.9. Интерфейсы в Go|Интерфейсы в Go]]"*
# Задача 1

**Формулировка:**
Создайте интерфейс `Printer` и реализуйте его у структуры `User`.
Пройдитесь по списку пользователей и вызовите метод `Print()`.

- Интерфейс `Printer` с методом `Print()`.
- Структура `User` с полями `Name`, `Age`.
- Метод `Print()` выводит `"Name Age"`.
- Создать `[]Printer` с тремя пользователями:
    - Vasya — 19
    - Masha — 21
    - Petr — 30
- Пройти циклом и вызвать `Print()` у каждого.

**Решение:**
```Go
package main
import "fmt"
type Printer interface {
    Print()
}
type User struct {
    Name string
    Age int
}
func (u User) Print() {
    fmt.Println(u.Name, u.Age)
}
func main() {
    p := []Printer{
        User{"Vasya", 19},
        User{"Masha", 21},
        User{"Petr", 30},
    }
    for i := range p {
        p[i].Print()
    }
}
```
**Вывод:**
```Run
Vasya 19
Masha 21
Petr 30
```

# Задача 2

**Формулировка:**
Создайте интерфейс `Pricer`, который возвращает стоимость объекта.

Подсчитайте общую сумму для разных типов.

- Интерфейс `Pricer` с методом `Price() int`.
- Структуры:
    - `Product` (`Name`, `UnitPrice`)
    - `Service` (`Name`, `Minutes`, `RatePerMinute`)
- Реализуйте `Price()` для обоих типов.
- Создайте `[]Pricer`:
    - Product: Phone — 1200
    - Product: Case — 200
    - Service: Repair — 30 минут × 10 руб.
- Посчитайте общую сумму.

**Решение:**
```Go
package main
import "fmt"
type Pricer interface {
    Price()int
}
type Product struct {
    Name string
    UnitPrice int
}
type Service struct {
    Name string
    Minutes int
    RatePerMinute int
}
func (u Product)Price()int{
    return u.UnitPrice
}
func (s Service)Price()int{
    return (s.Minutes*s.RatePerMinute)
}
func main() {
    p := []Pricer{
        Product{"Phone", 1200},
        Product{"Case", 200},
        Service{"Repair", 30, 10},
    }
    result := 0
    for i := range p {
        result += p[i].Price()
    }
    fmt.Printf("Итого: %v\n", result)
}
```
**Вывод:**
```Run
Итого: 1700
```

# Задача 3

**Формулировка:**
Проверьте разницу между методами на значении и указателе при реализации интерфейса.

- Интерфейс `Updater` с методом `UpdateAge()`.
- Структура `User` с полем `Age`.
- Версия 1: `func (u User) UpdateAge()` — увеличивает возраст на 1.
- Версия 2: `func (u *User) UpdateAge()` — увеличивает возраст на 1.
- Протестировать обе версии:
    - Создать `User{Age: 25}`
    - Вызвать через интерфейс метод и вывести возраст до и после.

**Решение:**
```Go
package main  
import "fmt"  
type Updater interface{ UpdateAge() }  
type User struct {  
    Age int  
}  
func (u *User) UpdateAge() {  
    u.Age++  
}  
func main() {  
    u := User{  
       Age: 10,  
    }  
    u.UpdateAge()  
    fmt.Println(u.Age)  
}
```
**~ Ответ ~**
```
При компиляции с функцией с указателем, значение изменилось.
```

# Задача 4

**Формулировка:**
Создайте срез `[]interface{}`, в который можно поместить значения разных типов.

- Создать срез `[]interface{}` из элементов:
    - `"Go"`
    - `10`
    - `true`
    - `3.14`
- Пройти по срезу циклом и вывести значение и его тип

**Решение:**
```Go
package main  
import "fmt"  
func main() {  
    items := []interface{}{"Go", 25, true, 3.14}  
  
    for i, v := range items {  
       fmt.Printf("[%d] %v (тип %T)\n", i, v, v)  
    }  
}
```
**Вывод:**
```Run
[0] Go (тип string)
[1] 25 (тип int)
[2] true (тип bool)
[3] 3.14 (тип float64)
```

# Задача 5

**Формулировка:**
Подсчитайте, сколько элементов каждого типа находится в срезе.

- Создать `[]interface{}`: `"hi"`, `2`, `"go"`, `4`, `true`, `7`.
- Пройти циклом и считать:
    - сколько строк (`string`);
    - сколько чисел (`int`);
    - сколько булевых (`bool`).

**Решение:**
```Go
package main  
import "fmt"  
func main() {  
    items := []interface{}{"hi", 2, "go", 4, true, 7}  
    var s, i, b int  
    for _, v := range items {  
       switch v.(type) {  
       case string:  
          s++  
       case int:  
          i++  
       case bool:  
          b++  
       }  
    }  
    fmt.Printf("strings: %v\nints: %v\nbools: %v\n", s, i, b)  
}
```
**Вывод:**
```Run
strings: 2
ints: 3
bools: 1
```

# Задача 6

**Формулировка:**
Напишите функцию `PrintAll`, которая принимает `[]interface{}` и печатает каждый элемент с его порядковым номером и типом.

```go
func PrintAll(values []interface{}
```
- Для каждого элемента вывести:
```
[номер] значение (тип тип_данных)
```
- Пример входных данных: `[]interface{}{"go", 42, false}`
- Проверить работу функции.

**Решение:**
```Go
package main
import "fmt"
func PrintAll(iface []interface{}) {
    for i := range iface {
        fmt.Printf("Номер: %v\nЗначение: %v\nТип данных: %T\n~ ~ ~ ~ ~\n", i, iface[i], iface[i])
    }
}
func main() {
    iface := []interface{}{"go", 42, false, 4.3}
    PrintAll(iface)
}
```
**Вывод:**
```Run
Номер: 0
Значение: go
Тип данных: string
~ ~ ~ ~ ~
Номер: 1
Значение: 42
Тип данных: int
~ ~ ~ ~ ~
Номер: 2
Значение: false
Тип данных: bool
~ ~ ~ ~ ~
Номер: 3
Значение: 4.3
Тип данных: float64
~ ~ ~ ~ ~
```

# Задача 7

**Формулировка:**
Создайте карту `map[string]interface{}`, которая хранит значения разных типов (строки, числа, логические значения).

- Добавить ключи:
    - `"name"` → `"Vasya"`
    - `"age"` → `25`
    - `"active"` → `true`
- Пройти по карте и вывести каждый ключ, значение и тип:
```
name: Vasya (string)
age: 25 (int)
active: true (bool)
```

**Решение:**
```Go
package main
import "fmt"
func main() {
    m := map[string]interface{}{"Name": "Vasya", "Age": 25, "Active": true,}
    for p, v := range m {
        fmt.Printf("%v: %v (%T)\n", p, v, v)
    }
}
```
**Вывод:**
```Run
Name: Vasya (string)
Age: 25 (int)
Active: true (bool)
```

# Задача 8

**Формулировка:**
Покажите, что интерфейс с `nil`-значением и полностью `nil`-интерфейс — это разные вещи.

```go
var a interface{}
var b interface{} = (*int)(nil)
fmt.Println(a == nil)
fmt.Println(b == nil)
```

**Решение:**
```Go
package main
import "fmt"
func main() {
    var a interface{}
    var b interface{} = (*int)(nil)
    fmt.Println(a == nil)
    fmt.Println(b == nil)
}
```
**Вывод:**
```Run
true
false
```

# Задача 9

**Формулировка:**
Попробуйте извлечь значение из пустого интерфейса как `int` и как `string`.

- Создать `var x interface{} = 42`.
- Извлечь как `int` → вывести `"ok int: 42"`.
- Извлечь как `string` → вывести `"not string"`.
- Использовать безопасную форму `value, ok := x.(T)`.

**Решение:**
```Go
package main
import "fmt"
func main() {
    var x interface{} = 42
    var value any
    var ok bool
    value, ok = x.(int)
    if ok {
        fmt.Println("type int:", value)
    } else {
        fmt.Println("not type int")
    }
    value, ok = x.(string)
    if ok {
        fmt.Println("type string:", value)
    } else {
        fmt.Println("not type string")
    }
}
```
**Вывод:**
```Run
type int: 42
not type string
```

# Задача 10

**Формулировка:**
Создайте функцию, которая принимает `interface{}` и печатает тип значения в зависимости от того, что хранится внутри.

```go
func DetectType(v interface{})
```

- Если это `int` → вывести `"Число"`
- Если `string` → `"Строка"`
- Если `bool` → `"Булево"`
- Иначе → `"Неизвестный тип"`
- Проверить на примерах: `10`, `"hi"`, `true`, `3.14`.

**Решение:**
```Go
package main
import "fmt"
func DetectType(v []interface{}){
    for i := range v {
        switch x := v[i].(type) {
        case int:
            fmt.Println("Число:", x)
        case string:
            fmt.Println("Строка:", x)
        case bool:
            fmt.Println("Булево:", x)
        default:
            fmt.Println("Неизвестный тип:", x)
        }
    }
}
func main() {
    v := []interface{}{10, "hi", true, 3.14}
    DetectType(v)
}
```
**Вывод:**
```Run
Число: 10
Строка: hi
Булево: true
Неизвестный тип: 3.14
```

# Задача 11

**Формулировка:**
Создайте функцию, которая принимает два значения `interface{}` и складывает их, если оба — числа.

```go
func Add(a, b interface{})
```

- Если оба `int` → вывести их сумму.
- Если оба `float64` → вывести их сумму.
- Иначе → `"Невозможно сложить"`.
- Пример вызовов:
    - `Add(3, 4)`
    - `Add(2.5, 3.5)`
    - `Add(3, "hi")`

**Решение:**
```Go
package main
import "fmt"
func Add(a, b interface{}){
    ai, aOk := a.(int)
    bi, bOk := b.(int)
    if aOk && bOk {
        fmt.Println(ai+bi)
        return
    }
    af, aOk := a.(float64)
    bf, bOk := b.(float64)
    if aOk && bOk {
        res := af+bf
        fmt.Printf("%.2f\n", res)
        return
    }
    fmt.Println("Невозможно сложить")
    return
}
func main() {
    Add(3, 4)
    Add(2.5, 3.5)
    Add(3, "hi")
}
```
**Вывод:**
```Run
7
6.00
Невозможно сложить
```

# Задача 12

**Формулировка:**
Подсчитайте, сколько в срезе разных типов: `int`, `string`, `bool`.

- Создать `[]interface{}{1, "go", true, 2, "hi", 3}`.
- Использовать `type switch` внутри цикла.
- Подсчитать количество каждого типа.

**Решение:**
```Go
package main
import "fmt"
func countOfTypes(s []interface{}) map[string]int {
    m := map[string]int{}
    for _, v := range s {
        switch v.(type) {
        case int:
            m["int"]++
        case string:
            m["string"]++
        case bool:
            m["bool"]++
        }
    }
    return m
}
func main() {
    s := []interface{}{1, "go", true, 2, "hi", 3}
    sTypes := countOfTypes(s)
    for r, v := range sTypes {
        fmt.Printf("%v: %v\n", r, v)
    }
}
```
**Вывод:**
```Run
int: 3
string: 2
bool: 1
```

# Задача 13

**Формулировка:**
Создайте функцию `PrintAnySlice`, которая принимает `[]interface{}` и печатает каждый элемент по-разному в зависимости от его типа.

```go
func PrintAnySlice(items []interface{})
```

- Если `string` → `"строка: VALUE"`
- Если `int` → `"число: VALUE"`
- Если `bool` → `"булево: VALUE"`
- Иначе → `"неизвестный тип"`
- Пример входных данных:
    `[]interface{}{"go", 5, true, 3.14}`

**Решение:**
```Go
package main  
import "fmt"  
func PrintAnySlice(items []interface{}) {  
    for _, i := range items {  
       switch x := i.(type) {  
       case string:  
          fmt.Printf("Строка: %v\n", x)  
       case int:  
          fmt.Printf("Число: %v\n", x)  
       case bool:  
          fmt.Printf("Булевое: %v\n", x)  
       default:  
          fmt.Println("Неизвестный тип")  
       }  
    }  
}  
func main() {  
    s := []interface{}{"go", 5, true, 3.14}  
    PrintAnySlice(s)  
}
```
**Вывод:**
```Run
Строка: go
Число: 5
Булевое: true
Неизвестный тип
```

# Задача 14

**Формулировка:**
Разделите значения из среза `[]interface{}` по типам: числа (`ints`), строки (`strings`), булевы (`bools`).

- Срез: `[]interface{}{"hi", 5, true, "go", 8, false}`.
- Пройти циклом, используя `type switch`.
- Наполнить три отдельных среза.
- Вывести каждый срез отдельно.

**Решение:**
```Go
package main  
import "fmt"  
func SplitTypes(items []interface{}) ([]interface{}, []interface{}, []interface{}) {  
    var sString, sInt, sBool []interface{}  
    for _, i := range items {  
       switch x := i.(type) {  
       case string:  
          sString = append(sString, x)  
       case int:  
          sInt = append(sInt, x)  
       case bool:  
          sBool = append(sBool, x)  
       default:  
          continue  
       }  
    }  
    return sString, sInt, sBool  
}  
func main() {  
    s := []interface{}{"hi", 5, true, "go", 8, false}  
    sString, sInt, sBool := SplitTypes(s)  
    fmt.Printf("String slice: %v\nInt slice: %v\nBool slice: %v\n", sString, sInt, sBool)  
}
```
**Вывод:**
```Run
String slice: [hi go]
Int slice: [5 8]
Bool slice: [true false]
```

# Задача 15

**Формулировка:**
Создайте интерфейсы `Reader` и `Writer`, а затем объедините их в `ReadWriter`.

- `Reader` с методом `Read() string`.
- `Writer` с методом `Write(data string)`.
- `ReadWriter` = `Reader + Writer`.
- Создать структуру `Note` с полем `text string`, реализующую оба метода.
- В `main`:
    - Записать текст `"Hello"`;
    - Прочитать его и вывести.

**Решение:**
```Go
package main  
import "fmt"  
type Reader interface {  
    Read() string  
}  
type Writer interface {  
    Write(data string)  
}  
type ReadWriter interface {  
    Reader  
    Writer}  
func (f *Note) Write(data string) {  
    f.text = data  
}  
func (f *Note) Read() string {  
    return f.text  
}  
type Note struct {  
    text string  
}  
func main() {  
    var f Note  
    f.Write("Hello")  
    fmt.Println(f.Read())  
}
```
**Вывод:**
```Run
Hello
```

# Задача 16

**Формулировка:**
Добавьте интерфейс `Logger` и реализуйте его в структуре `App`.

- Интерфейс `Logger` с методом `Log(msg string)`.
- Структура `ConsoleLogger`, выводящая `[LOG]: msg`.
- Структура `App` с полями:
    - `Name string`
    - `Logger Logger`
- Метод `Run()` выводит сообщение `"App <Name> started"` через логгер.

**Решение:**
```Go
package main  
import "fmt"  
type Logger interface {  
    Log(msg string)  
}  
type ConsoleLogger struct{}  
func (c *ConsoleLogger) Log(msg string) {  
    fmt.Println("[LOG]:", msg)  
}  
type App struct {  
    Name   string  
    Logger Logger  
}  
func (a *App) Run() {  
    a.Logger.Log(fmt.Sprintf("App %v started", a.Name))  
}  
func main() {  
    c := &ConsoleLogger{}  
    a := App{  
       Name:   "GoApp",  
       Logger: c,  
    }  
    a.Run()  
}
```
**Вывод:**
```Run
[LOG]: App GoApp started
```

# Задача 17

**Формулировка:**
Покажите, как можно подменить реализацию интерфейса.

- Интерфейс `Logger` (метод `Log(msg string)`).
- Две реализации:
    - `ConsoleLogger` — пишет в консоль.
    - `SilentLogger` — ничего не делает.
- Структура `App` с `Logger`.
- Сначала запустить с `ConsoleLogger`, потом с `SilentLogger`.

**Решение:**
```Go
package main  
import "fmt"  
type Logger interface {  
    Log(msg string)  
}  
type ConsoleLogger struct{}  
type SilentLogger struct{}  
func (c *ConsoleLogger) Log(msg string) {  
    fmt.Println("[LOG]:", msg)  
}  
func (c *SilentLogger) Log(msg string) {  
    return  
}  
type App struct {  
    Name   string  
    Logger Logger  
}  
func (a *App) Run() {  
    a.Logger.Log(fmt.Sprintf("App %v started", a.Name))  
}  
func main() {  
    c := &ConsoleLogger{}  
    a := App{  
       Name:   "GoApp",  
       Logger: c,  
    }  
    a.Run()  
    c2 := &SilentLogger{}  
    a2 := App{  
       Name:   "GoApp",  
       Logger: c2,  
    }  
    a2.Run()  
}
```
**Вывод:**
```Run
[LOG]: App GoApp started
```

# Задача 18

**Формулировка:**
Создайте интерфейс `Notifier`, который объединяет `Sender` и `Formatter`.

- Интерфейс `Sender` с методом `Send(msg string)`.
- Интерфейс `Formatter` с методом `Format(msg string) string`.
- Интерфейс `Notifier`, объединяющий их.
- Структура `EmailNotifier`, реализующая оба метода:
    - `Format` → добавляет префикс `"EMAIL:"`;
    - `Send` → печатает результат форматирования.

**Решение:**
```Go
package main
import "fmt"
type Sender interface {
    Send(msg string)
}
type Formatter interface {
    Format(msg string)string
}
type Notifier interface {
    Sender
    Formatter
}
type EmailNotifier struct {}

func (e EmailNotifier) Format(msg string)string {
    return "EMAIL: " + msg
}
func (e EmailNotifier) Send(msg string) {
    fmt.Println(e.Format(msg))
}
func main() {
    e := EmailNotifier{}
    e.Send("Hello!")
}
```
**Вывод:**
```Run
EMAIL: Hello!
```

# Задача 19

**Формулировка:**
Создайте интерфейс `Logger` и объедините его с `Writer` в интерфейсе `LogWriter`.

- `Logger` → метод `Log(msg string)`
- `Writer` → метод `Write(data string)`
- `LogWriter` объединяет оба.
- Создать структуру `ConsoleWriter`, реализующую оба метода.
- Вызвать `Write("Data")` и `Log("Event")`.

**Решение:**
```Go
package main
import "fmt"
type Logger interface {
    Log(msg string)
}
type Writer interface {
    Write(data string)
}
type LogWriter interface {
    Logger
    Writer
}
type ConsoleWriter struct {}
func (c ConsoleWriter) Log(msg string) {
    fmt.Println("[LOG]:", msg)
}
func (c ConsoleWriter) Write(data string) {
    fmt.Println("[WRITE]:", data)
}
func main() {
    c := ConsoleWriter{}
    c.Write("Data")
    c.Log("Event")
}
```
**Вывод:**
```Run
[WRITE]: Data
[LOG]: Event
```

# Задача 20

**Формулировка:**
Создайте структуру `Server`, использующую интерфейс `Logger` для вывода сообщений.

- Интерфейс `Logger` с методом `Log(msg string)`.
- Реализация `ConsoleLogger`.
- Структура `Server` с полями `Name string`, `Logger Logger`
- Метод `Start()` выводит `"<Name> started"`.
- Метод `Stop()` выводит `"<Name> stopped"`.
- Проверить на примере `Server{"Main"}`.

**Решение:**
```Go
package main
import "fmt"
type Logger interface {
    Log(msg string)
}
type ConsoleLogger struct{}
type Server struct {
    Name string
    Logger Logger
}
func (c ConsoleLogger) Log(msg string) {
    fmt.Println(msg)
}
func (s Server) Start(){
    s.Logger.Log(s.Name + " started")
}
func (s Server) Stop(){
    s.Logger.Log(s.Name + " stopped")
}
func main() {
    s := Server{
        Name: "Main",
        Logger: ConsoleLogger{},
    }
    s.Start()
    s.Stop()
}
```
**Вывод:**
```Run
Main started
Main stopped
```

# Задача 21

**Формулировка:**
Создайте интерфейс `Logger` с методом `Log(msg string)` и несколько его реализаций.

- `Logger` — интерфейс с методом `Log(msg string)`.
- Реализации:
    - `ConsoleLogger` — печатает `[LOG]: msg`.
    - `FileLogger` — сохраняет сообщение в поле `History []string`.
- Функция `Run(log Logger)` принимает интерфейс и вызывает `log.Log("Started")`.

**Решение:**
```Go
package main
import "fmt"
type Logger interface {
    Log(msg string)
}
type ConsoleLogger struct{}
type FileLogger struct{
    History []string
}
func (c ConsoleLogger) Log(msg string) {
    fmt.Println("[LOG]:",msg)
}
func (f *FileLogger) Log(msg string) {
    f.History = append(f.History, msg)
}
func Run(log Logger) {
    log.Log("Started")
}
func main() {
    Run(ConsoleLogger{})
}
```
**Вывод:**
```Run
[LOG]: Started
```

# Задача 22

**Формулировка:**
Создайте интерфейс `Payment` и несколько способов оплаты.

- Интерфейс `Payment` с методом `Pay(amount int)`.
- Реализации:
    - `CardPayment` — печатает `"Оплата картой: amount"`.
    - `CashPayment` — печатает `"Оплата наличными: amount"`.
- Функция `Process(p Payment)` вызывает `p.Pay(100)`.

**Решение:**
```Go
package main  
import "fmt"  
type Payment interface {  
    Pay(amount int)  
}  
type CardPayment struct{}  
type CashPayment struct{}  
func (cash CashPayment) Pay(amount int) {  
    fmt.Println("Оплата наличными:", amount)  
}  
func (card CardPayment) Pay(amount int) {  
    fmt.Println("Оплата картой:", amount)  
}  
func Process(p Payment) {  
    p.Pay(100)  
}  
func main() {  
    card := CardPayment{}  
    cash := CashPayment{}  
    Process(card)  
    Process(cash)  
}
```
**Вывод:**
```Run
Оплата картой: 100
Оплата наличными: 100
```

# Задача 23

**Формулировка:**
Создайте интерфейс `Storage`, который может сохранять и извлекать данные любых типов.

- Интерфейс:
    
    ```go
    type Storage interface {
        Save(key string, value interface{})
        Get(key string) interface{}
    }
    ```
    
- Реализация `MemoryStorage` на основе `map[string]interface{}`.
    
- Сохранить:
    
    - `"user"` → `"Vasya"`
    - `"age"` → `25`
- Извлечь и вывести с проверкой типов через `type switch`.

**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 24



**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 25



**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 26



**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 27



**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 28



**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 29



**Решение:**
```Go

```
**Вывод:**
```Run

```

