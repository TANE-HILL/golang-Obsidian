_Практика по итогу модуля_  
*"[[1.15. Каналы и конструкция Select|Каналы и конструкция Select]]"*

# Задача 1

(в понедельник скинуть файлы с рабочего компа)

**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 2

(в понедельник скинуть файлы с рабочего компа)

**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 3

(в понедельник скинуть файлы с рабочего компа)

**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 4

**Формулировка:**
Горутина должна начать выполнение **только после сигнала** из `main`.
После получения сигнала она выводит `"Работа началась!"` и завершает выполнение.

**Функция для реализации:**
```go
func waitForSignal() string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(waitForSignal())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func waitForSignal() string {  
    ch1 := make(chan string)  
    start := make(chan bool)  
    go func() {  
       <-start  
       close(start)  
       ch1 <- "Работа началась!"  
    }()  
    start <- true  
    return <-ch1  
}  
/*  
Сделать то, что просит условие,  
невозможно. Чтобы "подать сигнал  
в горутину из main", необходимо  
поменять сигнатуры waitForSignal  
и main. Сделать так, чтобы горутина  
создавалась в main. Тогда с помощью  
канала можно будет передать  
сигнал. Иначе - только так.
*/  
func main() {  
    fmt.Println(waitForSignal())  
}
```
**Вывод:**
```Run
Работа началась!
```

# Задача 5

**Формулировка:**
Создайте две горутины, которые последовательно обрабатывают данные:

1. первая отправляет `"data"`,
2. вторая принимает и возвращает `"processed data"`.

**Функция для реализации:**
```go
func pipeline() string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(pipeline())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func pipeline() string {  
    ch1 := make(chan string)  
    ch2 := make(chan string)  
    go func() {  
       ch1 <- "data"  
    }()  
    go func() {  
       ch2 <- "processed " + <-ch1  
    }()  
    return <-ch2  
}  
func main() {  
    fmt.Println(pipeline())  
}
```
**Вывод:**
```Run
processed data
```

# Задача 6

**Формулировка:**
Три горутины выполняют разные задачи и отправляют сигнал `true` в канал `done`.
Программа должна дождаться всех трёх сигналов и вернуть `"Все завершены"`.

**Функция для реализации:**
```go
func waitAll() string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(waitAll())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func waitAll() string {  
    done := make(chan bool, 3)  
    go func() {  
       fmt.Printf("все ")  
       done <- true  
    }()  
    go func() {  
       fmt.Printf("горутины ")  
       done <- true  
    }()  
    go func() {  
       fmt.Printf("успешно ")  
       done <- true  
    }()  
    for i := 1; i <= 3; i++ {  
       <-done  
    }  
    return "завершены"  
}  
func main() {  
    fmt.Printf(waitAll())  
}
```
**Вывод:**
```Run
успешно горутины все завершены
```

# Задача 1

**Формулировка:**
Создайте буферизированный канал на 3 числа.
Передайте в него значения от 1 до 3 и выведите их через `range`.

**Функция для реализации:**
```go
func readNumbers() []int {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(readNumbers())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func readNumbers() []int {  
    ch := make(chan int, 3)  
    result := []int{}  
    go func() {  
       for i := 1; i <= 3; i++ {  
          ch <- i  
       }  
       close(ch)  
    }()  
    for val := range ch {  
       result = append(result, val)  
    }  
    return result  
}  
func main() {  
    fmt.Println(readNumbers())  
}
```
**Вывод:**
```Run
[1 2 3]
```

# Задача 2

**Формулировка:**
Создайте горутину, которая отправляет в канал 5 сообщений строкового типа.
Основная функция должна собрать их в срез с помощью `range`.

**Функция для реализации:**
```go
func messageStream() []string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(messageStream())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func messageStream() []string {  
    ch := make(chan string, 5)  
    result := []string{}  
    go func() {  
       ch <- "msg1"  
       ch <- "msg2"  
       ch <- "msg3"  
       ch <- "msg4"  
       ch <- "msg5"  
       close(ch)  
    }()  
    for val := range ch {  
       result = append(result, val)  
    }  
    return result  
}  
func main() {  
    fmt.Println(messageStream())  
}
```
**Вывод:**
```Run
[msg1 msg2 msg3 msg4 msg5]
```

# Задача 3

**Формулировка:**
Создайте функцию, которая получает числа из канала и возвращает их сумму после закрытия канала.

**Функция для реализации:**
```go
func sumChannel() int {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(sumChannel()) // 15
}
```

**Решение:**
```Go
package main  
import "fmt"  
func sumChannel() int {  
    ch, res := make(chan int, 5), 0  
    go func() {  
       for i := 1; i <= 5; i++ {  
          ch <- i  
       }  
       close(ch)  
    }()  
    for val := range ch {  
       res += val  
    }  
    return res  
}  
func main() {  
    fmt.Println(sumChannel()) // 15  
}
```
**Вывод:**
```Run
15
```

# Задача 4

**Формулировка:**
Создайте два отправителя, каждый из которых записывает по 3 числа в один канал.
Закройте канал, когда все отправки завершены, и выведите все значения через range.

**Функция для реализации:**
```go
func multiSender() []int {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(multiSender())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func multiSender() []int {  
    ch, done := make(chan int, 6), make(chan bool, 2)  
    res := []int{}  
    go func() {  
       for i := 4; i <= 6; i++ {  
          ch <- i  
       }  
       done <- true  
    }()  
    go func() {  
       for i := 1; i <= 3; i++ {  
          ch <- i  
       }  
       done <- true  
    }()  
    <-done  
    <-done  
    close(ch)  
    for val := range ch {  
       res = append(res, val)  
    }  
    return res  
}  
func main() {  
    fmt.Println(multiSender())  
}
```
**Вывод:**
```Run
[1 2 3 4 5 6]
```

# Задача 5

**Формулировка:**
В канал отправляются задания (строки “task 1”, “task 2” и т. д.).
Когда задания закончились — канал закрывается, а функция возвращает строку `"all tasks processed"` после чтения всех данных.

**Функция для реализации:**
```go
func processTasks() string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(processTasks())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func processTasks() string {  
    ch := make(chan string)  
    go func() {  
       ch <- "task1"  
       ch <- "task2"  
       ch <- "task3"  
       ch <- "task4"  
       ch <- "task5"  
       close(ch)  
    }()  
    for val := range ch {  
       fmt.Println(val)  
    }  
    return "all tasks processed\n"  
}  
func main() {  
    fmt.Println(processTasks())  
}
```
**Вывод:**
```Run
task1
task2
task3
task4
task5
all tasks processed
```

# Задача 6

**Формулировка:**
Создайте канал, в который горутина записывает числа от 1 до 10.
Если встречается число `5`, чтение прекращается, и функция возвращает только считанные до этого числа значения.

**Функция для реализации:**
```go
func stopOnFive() []int {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(stopOnFive())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func stopOnFive() []int {  
    ch, res := make(chan int), []int{}  
    go func() {  
       for i := 1; i <= 10; i++ {  
          ch <- i  
       }  
    }()  
    for val := range ch {  
       if val == 5 {  
          break  
       }  
       res = append(res, val)  
    }  
    return res  
}  
func main() {  
    fmt.Println(stopOnFive())  
}
```
**Вывод:**
```Run
[1 2 3 4]
```

# Задача 7

**Формулировка:**
Создайте буферизированный канал на 2 сообщения.
Отправьте в него два слова подряд, а затем выведите их по порядку.

**Функция для реализации:**
```go
func mailbox() []string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(mailbox())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func mailbox() []string {  
    ch, res := make(chan string, 2), []string{}  
    ch <- "first"  
    ch <- "second"  
    res = append(res, <-ch)  
    res = append(res, <-ch)  
    return res  
}  
func main() {  
    fmt.Println(mailbox())  
}
```
**Вывод:**
```Run
[first second]
```

# Задача 8

**Формулировка:**
Создайте канал вместимостью 1.
Отправьте два значения подряд и посмотрите, что произойдёт.

**Функция для реализации:**
```go
func overflowTest() string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(overflowTest())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func overflowTest() string {  
    ch, res := make(chan string, 1), ""  
    ch <- "first"  
    ch <- "second"  
    res = <-ch  
    return res  
}  
func main() {  
    fmt.Println(overflowTest())  
}
```
**Вывод:**
```Run
fatal error: all goroutines are asleep - deadlock!
```

# Задача 9

**Формулировка:**
Реализуйте очередь из 3 сообщений, которые приходят в канал, и функция должна вернуть их все в порядке приёма.

**Функция для реализации:**
```go
func messageQueue() []string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(messageQueue())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func messageQueue() []string {  
    ch, res := make(chan string, 3), []string{}  
    ch <- "one"  
    ch <- "two"  
    ch <- "three"  
    res = append(res, <-ch, <-ch, <-ch)  
    return res  
}  
func main() {  
    fmt.Println(messageQueue())  
}
```
**Вывод:**
```Run
[one two three]
```

# Задача 10

**Формулировка:**
Горутина принимает числа через канал, суммирует их и возвращает результат в другой канал.

**Функция для реализации:**
```go
func sumBuffered(nums []int) int {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(sumBuffered([]int{2, 4, 6})) // 12
}
```

**Решение:**
```Go
package main  
import "fmt"  
func sumBuffered(nums []int) int {  
    ch1, ch2 := make(chan int, 3), make(chan int)  
    for _, v := range nums {  
       ch1 <- v  
    }  
    go func() {  
       res := 0  
       for v := range ch1 {  
          res += v  
       }  
       ch2 <- res  
  
    }()  
    close(ch1)  
    return <-ch2  
}  
func main() {  
    fmt.Println(sumBuffered([]int{2, 4, 6})) // 12  
}
```
**Вывод:**
```Run
12
```

# Задача 11

**Формулировка:**
Один поток (“производитель”) отправляет 5 сообщений в буферизированный канал.
Второй поток (“потребитель”) их считывает и выводит по мере готовности.

**Функция для реализации:**
```go
func producerConsumer() []string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(producerConsumer())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func producerConsumer() []string {  
    ch, resCh := make(chan string, 5), make(chan []string)  
    go func() {  
       ch <- "msg1"  
       ch <- "msg2"  
       ch <- "msg3"  
       ch <- "msg4"  
       ch <- "msg5"  
    }()  
    go func() {  
       res := []string{<-ch, <-ch, <-ch, <-ch, <-ch}  
       resCh <- res  
    }()  
    return <-resCh  
}  
func main() {  
    fmt.Println(producerConsumer())  
}
```
**Вывод:**
```Run
[msg1 msg2 msg3 msg4 msg5]
```

# Задача 12

**Формулировка:**
Создайте канал, передайте в него несколько сообщений и закройте.
Функция должна вернуть все сообщения в виде среза.

**Функция для реализации:**
```go
func readAll() []string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(readAll())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func readAll() []string {  
    ch, res := make(chan string), []string{}  
    go func() {  
       ch <- "one"  
       ch <- "two"  
       ch <- "three"  
       close(ch)  
    }()  
    for val := range ch {  
       res = append(res, val)  
    }  
    return res  
}  
func main() {  
    fmt.Println(readAll())  
}
```
**Вывод:**
```Run
[one two three]
```

# Задача 13

**Формулировка:**
Создайте закрытый канал и попробуйте считать из него значение.
Функция должна вернуть `"closed"`, если канал закрыт.

**Функция для реализации:**
```go
func checkClosed() string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(checkClosed())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func checkClosed() string {  
    ch := make(chan int, 1)  
    close(ch)  
    val, ok := <-ch  
    if !ok {  
       return "Канал закрыт"  
    } else {  
       fmt.Println("Получено значение:", val)  
       return "Канал открыт"  
    }  
}  
func main() {  
    fmt.Println(checkClosed())  
}
```
**Вывод:**
```Run
Канал закрыт
```

# Задача 14

**Формулировка:**
Горутина должна выполнить задачу, а затем послать сигнал о завершении через канал `done`. Основная функция ждёт этот сигнал и возвращает `"done"`.

**Функция для реализации:**
```go
func signalDone() string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(signalDone())
}
```

**Решение:**
```Go
package main  
import (  
    "fmt"  
    "time")  
func signalDone() string {  
    done := make(chan bool)  
    go func() {  
       time.Sleep(time.Second)  
       done <- true  
    }()  
    <-done  
    return "done"  
}  
func main() {  
    fmt.Println(signalDone())  
}
```
**Вывод:**
```Run
done
```

# Задача 15

**Формулировка:**
Реализуйте горутину, которая передаёт три числа в канал `data`, затем закрывает его и отправляет сигнал в канал `done`.
Функция должна вернуть `"finished"` после получения сигнала.

**Функция для реализации:**
```go
func multiSignal() string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(multiSignal())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func multiSignal() string {  
    data, done := make(chan int, 3), make(chan bool)  
    go func() {  
       data <- 1  
       data <- 2  
       data <- 3  
       close(data)  
       done <- true  
    }()  
    <-done  
    return "finished"  
}  
func main() {  
    fmt.Println(multiSignal())  
}
```
**Вывод:**
```Run
finished
```

# Задача 16

**Формулировка:**
Создайте функцию, которая закрывает канал только один раз.
Если попытаться закрыть его повторно, функция должна вернуть `"panic prevented"`.

**Функция для реализации:**
```go
func safeClose() string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(safeClose())
}
```

**Решение:**
```Go
package main  
import "fmt"  
func safeClose() string {  
    defer func() {  
       if r := recover(); r != nil {  
          fmt.Println("panic prevented")  
       }  
    }()  
    ch := make(chan int)  
    close(ch)  
    close(ch)  
    return "done"  
}  
func main() {  
    fmt.Println(safeClose())  
}
```
**Вывод:**
```Run
panic prevented
```

# Задача 17

**Формулировка:**
Одна горутина отправляет данные и закрывает канал.
Несколько горутин-получателей должны прочитать всё, что осталось, и вернуть `"done"` после завершения чтения.

**Функция для реализации:**
```go
func multiReceiver() string {
    // ваш код здесь
}
```

**Пример main:**
```go
func main() {
    fmt.Println(multiReceiver())
}
```

**Решение:**
```Go

```
**Вывод:**
```Run

```

