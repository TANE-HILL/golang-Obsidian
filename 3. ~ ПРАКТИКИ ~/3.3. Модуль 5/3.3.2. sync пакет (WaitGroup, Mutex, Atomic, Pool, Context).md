_Практика по итогу модуля_  
*"[[1.4.2. sync пакет (WaitGroup, Mutex, Atomic, Pool, Context)|sync пакет (WaitGroup, Mutex, Atomic, Pool, Context)]]"*

# Задача 1

**Формулировка:**
Напиши программу, которая запускает одну горутину, выполняющую печать строки, и корректно ждёт её завершения с помощью `WaitGroup`.

- Создать `WaitGroup`.
- Добавить одну задачу через `Add()`.
- Запустить горутину, которая печатает `"Привет из горутины"` и вызывает `Done()`.
- В `main` вызвать `Wait()`.

**Каркас программы:**
```go
func main() {
    var wg sync.WaitGroup
    // TODO: добавить одну задачу
    // TODO: запустить горутину, которая печатает сообщение и вызывает Done()
    // TODO: дождаться завершения всех задач
}
```

**Решение:**
```Go
package main  
import (
    "fmt"
    "sync"
    )
func main() {
    var wg sync.WaitGroup
    wg.Add(1)
    go func(){
        defer wg.Done()
        fmt.Println("Привет!")
    }()
    wg.Wait()
    fmt.Println("Пока!")
}
```
**Вывод:**
```Run
Привет!
Пока!
```

# Задача 2

**Формулировка:**
Запусти **три горутины**, каждая из которых печатает свой номер. Дождись, пока все завершатся.

- Добавить 3 задачи в `WaitGroup`.
- Запустить три горутины, которые печатают `"Горутина X"`, где X — номер.
- В каждой горутине вызывать `Done()` по завершении.
- В `main` дождаться завершения всех.

**Каркас программы:**
```go
func main() {
    var wg sync.WaitGroup
    // TODO: добавить 3 задачи в группу
    for i := 1; i <= 3; i++ {
        // TODO: запустить горутину, которая печатает номер
    }
    // TODO: дождаться всех горутин
}
```

**Решение:**
```Go
package main  
import (
    "fmt"
    "sync"
    )
func main() {
    var wg sync.WaitGroup
    wg.Add(3)
    for i := 1; i <= 3; i++ {
        go func(){
            defer wg.Done()
            fmt.Println(i) 
        }()
    }
    wg.Wait()
}
```
**Вывод:**
```Run
3
1
2
```

# Задача 3

**Формулировка:**
Сравни подход через `done` канал и `WaitGroup`.

Создай две версии программы — обе запускают 3 горутины, но ждут разными способами.

- Первая часть — с `done` каналом.
- Вторая часть — с `WaitGroup`.
- Вывести пояснение: что проще и надёжнее.

**Каркас программы:**
```go
func main() {
    // 🟢 Вариант 1: через done канал
    // TODO: создайте done канал, запустите 3 горутины, каждая отправляет true
    // TODO: дождитесь 3 сигналов через <-done

    // 🟢 Вариант 2: через WaitGroup
    // TODO: создайте WaitGroup, добавьте 3 задачи
    // TODO: каждая горутина печатает сообщение и вызывает Done()
    // TODO: дождитесь завершения
}
```

**Решение:**
```Go
package main  
import (
    "fmt"
    "sync"
    )
func withWG(){
    var wg sync.WaitGroup
    wg.Add(3)
    for i := 1; i <= 3; i++ {
        go func(){
            defer wg.Done()
            fmt.Println("WaitGroup, задача:", i)
        }()
    }
    wg.Wait()
}
func withDone(){
    done := make(chan bool, 3)
    for i := 1; i <= 3; i++ {
        go func(){
            fmt.Println("Done, задача:", i)
            done <- true
        }()
    }
    <-done
    <-done
    <-done
}
/*
WaitGroup проще и надёжнее, чем метод с <-done.
~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~
В случае WaitGroup используются методы из пакета,
специально предназначенные для ожидания группы
горутин. Количество ожидаемых горутин записывается
в wg.Add(_) и все они будут ожидаться в wg.Wait.
~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~
В случае "Done" - нужно вручную создавать канал,
принимающий тип bool и вручную дожидаться передачи
от каждой горутины: <-done <-done <-done... и т.д.
*/
func main() {
    withWG()
    withDone()
}
```
**Вывод:**
```Run
WaitGroup, задача: 2
WaitGroup, задача: 3
WaitGroup, задача: 1
Done, задача: 3
Done, задача: 1
Done, задача: 2
```

# Задача 4

**Формулировка:**
Реализуй простую имитацию пула рабочих горутин, которые обрабатывают задачи (например, печать сообщений).

- Пусть есть 5 задач (строки в слайсе).
- Создай 3 горутины, каждая берёт задачу из канала и обрабатывает.
- Используй `WaitGroup`, чтобы дождаться завершения всех воркеров.

**Каркас программы:**
```go
func main() {
    tasks := []string{"one", "two", "three", "four", "five"}
    ch := make(chan string)
    var wg sync.WaitGroup
    // 🟢 Запуск воркеров
    for i := 1; i <= 3; i++ {
        wg.Add(1)
        go func(workerID int) {
            defer wg.Done()
            // TODO: получать задачи из канала и печатать
        }(i)
    }
    // 🟢 Отправка задач
    // TODO: отправить все задачи в канал и закрыть его
    // 🟢 Ожидание завершения всех воркеров
    // TODO: дождаться всех горутин
}
```

**Решение:**
```Go
package main  
import (
    "fmt"
    "sync"
    )
/*
Через цикл "for i" с помощью "go func" ОДНОВРЕМЕННО стартуют
три воркера: 1, 2, 3. Каждый из них останавливается на
"for task := range ch" и ждёт следующего значения пока канал
открыт. Когда новое значение поступает в канал, один из
воркеров берёт его в работу.
Когда канал закрывается, воркеры дорабатывают оставшиеся
значения и идут дальше (перестают ожидать новых значений).
*/
func main() {
    tasks := []string{"one", "two", "three", "four", "five"}
    ch := make(chan string)
    var wg sync.WaitGroup
    for i := 1; i <= 3; i++ {
        wg.Add(1)
        go func(workerID int) {
            defer wg.Done()
            for task := range ch {
                fmt.Printf("Worker %v: %s\n", workerID, task)
            }
        }(i)
    }
    for r := range tasks {
        ch <- tasks[r]
    }
    close(ch)
    wg.Wait()
}
```
**Вывод:**
```Run
Worker 3: one
Worker 3: four
Worker 3: five
Worker 2: three
Worker 1: two
```

---
<center>~ <font color="#ff0000">Data Race</font> ~</center>
___
# Задача 1

**Формулировка:**
Программа запускает две горутины, которые увеличивают одну и ту же переменную `counter`.

Посмотри на результат и объясни, почему он непредсказуем.

- Запустить 2 горутины, которые делают `counter++` 1000 раз.
- Вывести итоговое значение счётчика.
- Объяснить, почему результат разный при каждом запуске.

**Каркас программы:**
```go
func main() {
    var counter int
    for i := 0; i < 2; i++ {
        go func() {
            // TODO: увеличить counter 1000 раз
        }()
    }
    // TODO: подождать немного , чтобы горутины успели выполниться
    fmt.Println("Результат:", counter)
}
```

**Решение:**
```Go
package main  
import (
    "fmt"
    "time"
    )
func main() {
    var counter int
    for i := 0; i < 2; i++ {
        go func(){
          for range 1000 {
              counter++
          }  
        }()
    }
    time.Sleep(3 * time.Second)
    fmt.Println("Результат:", counter)
}
/*
В этом примере возникает гонка данных,
поскольку две горутины работают с 
одной операцией параллельно.
А операция не является не атомарной.

counter++ выполняется в три действия:
    1. Прочитать counter
    2. Прибавить 1
    3. Записать результат в counter

Из-за этого может возникнуть ситуация,
когда несколько горутин начинают
рабоать с одним значением одновременно.

Пусть это значение - 67, тогда
к концу итерации две горутины
запишут в counter число 68.
На этом моменте единица потерялась.
И таких "потерь" может быть много,
поэтому результат работы данной
программы непредсказуем. При ожидаемом
результате в "2000", мы можем получить,
например, "1653".
*/
```
**Вывод:**
```Run
Результат: 1653
```

# Задача 2

**Определить, что будет выведено:**
```go
package main
import (
	"fmt"
	"time"
)
func main() {
	var i int
	go func() {
		for j := 0; j < 1000; j++ {
			i++ // операция неатомарна
		}
	}()
	go func() {
		for j := 0; j < 1000; j++ {
			i++ // вторая горутина делает то же самое
		}
	}()
	time.Sleep(time.Second)
	fmt.Println("Результат:", i)
}
```

**Решение:**
```
Может быть выведено любое число до 2000, так как в этом коде возникает гонка данных - такая же, как в предыдущем примере.
```

# Задача 3

**Определить, что покажет программа при запуске с флагом `-race`:**
```go
package main

import (
	"fmt"
	"time"
)

func main() {
	var counter int

	for i := 0; i < 2; i++ {
		go func() {
			for j := 0; j < 1000; j++ {
				counter++ // потенциальная гонка
			}
		}()
	}

	time.Sleep(time.Second)
	fmt.Println("Итог:", counter)
}

```

**Решение:**
```
Метод -race при запуске укажет на наличие гонки данных в этой программе.
```

# Задача 4

**Определить, как изменится результат после устранения гонки:**
```go
package main

import (
	"fmt"
	"sync"
	"time"
)
func main() {
	// 🟥 Версия с гонкой
	var counter int
	for i := 0; i < 2; i++ {
		go func() {
			for j := 0; j < 1000; j++ {
				counter++ // небезопасная операция
			}
		}()
	}
	time.Sleep(time.Second)
	fmt.Println("С гонкой:", counter)
	
	// 🟩 Исправленная версия с Mutex
	var safeCounter int
	var mu sync.Mutex
	var wg sync.WaitGroup
	for i := 0; i < 2; i++ {
		wg.Add(1)
		go func() {
			defer wg.Done()
			for j := 0; j < 1000; j++ {
				mu.Lock()
				safeCounter++
				mu.Unlock()
			}
		}()
	}
	wg.Wait()
	fmt.Println("Без гонки:", safeCounter)
}
```

**Решение:**
```
В первом примере будут выводиться любые значения до 2000, так как присутствует гонка данных.
Во втором случае гонки данных нет, благодаря применению Mutex, поэтому результат вывода всегда предсказуем - ровно 2000.
```

# Задача 5



**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 6



**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 7



**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 8



**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 9



**Решение:**
```Go

```
**Вывод:**
```Run

```

# Задача 10



**Решение:**
```Go

```
**Вывод:**
```Run

```

