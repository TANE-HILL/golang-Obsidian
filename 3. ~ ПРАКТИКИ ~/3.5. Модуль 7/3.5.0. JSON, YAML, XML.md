_Практика по итогу модуля_  
*"[[1.6.0.0. Работа с JSON, YAML, XML|Работа с JSON, YAML, XML]]"*

# <u>Задание</u>

1. Возьмите структуру `Book` с полями `Title`, `Author`, `Year`.
2. Сериализуйте её в **JSON**, **YAML**, **XML** (выведите в консоль).
3. Сделайте обратное: из трёх строк (json/yaml/xml) распарсьте в `Book` и выведите `Title`.

---
# <u>Решение</u>

#### 1. <u>JSON</u>, <u>YAML</u>, <u>XML</u> **->** <u>Go</u>

```Go

```