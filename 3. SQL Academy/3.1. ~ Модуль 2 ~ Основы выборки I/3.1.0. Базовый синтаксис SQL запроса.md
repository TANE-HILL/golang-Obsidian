Одна из основных функций **SQL** — это получение выборок данных из СУБД. Для этого в SQL используется оператор `SELECT`.
Рассмотрим несколько простых запросов с его участием.

# Вывод произвольных значений

Через оператор `SELECT` можно выводить данные **не только из таблиц [[3.0.0. Что такое БД и СУБД|базы данных]]**, но и **произвольные строки, числа, даты и т.д.**

*Например, так можно вывести произвольную строку:*
```PostgreSQL 17.5
SELECT 'Hello world'
```

# Вывод всех данных из таблицы

Для вывода всех полей из определённой таблицы используется символ `*` 
Взглянем на схему базы данных и выведем данные одной из таблиц.

**Схема:**
![[Pasted image 20260909010616.png|539]]
**Запрос:**
```PostgreSQL 17.5
SELECT * FROM FamilyMembers
```
**Вывод:**
![[Pasted image 20260909010707.png]]

# Вывод данных из определенных колонок таблицы

Если необходимо вывести информацию только по определённым столбцам таблицы, а не всю сразу, то это можно сделать, перечисляя названия столбцов через запятую:
```PostgreSQL 17.5
SELECT member_id, member_name FROM FamilyMembers
```
**Вывод:**
![[Pasted image 20260909010909.png|412]]

# Псевдонимы

В случае, если мы хотим вывести какие-то столбцы таблицы, но чтобы в итоговой выборке они были названы иначе, мы можем использовать **псевдонимы** (их также называют **алиасами**).

Мы должны использовать оператор `AS`. Как в примере:
```PostgreSQL 17.5
SELECT member_id, member_name AS Name FROM FamilyMembers
```
**Вывод:**
![[Pasted image 20260909011103.png|471]]

Или же можно обойтись и без него, просто написав желаемое наименование поля через пробел.

```PostgreSQL 17.5
SELECT member_id, member_name Name FROM FamilyMembers
```
	Псевдонимы могут содержать до 255 знаков (включая пробелы, цифры и специальные символы).

