Оператор **` LIKE `** используется при [[3.1.4. Условный оператор WHERE|условных запросах]], когда мы хотим узнать, **соответствует ли строка определённому шаблону**.

Например, у нас есть таблица **` Users `**, в которой есть поле **` email `**:
```PostgreSQL 17.5
SELECT name, email FROM Users;
```

Допустим, мы хотим найти всех пользователей, **чья почта лежит в домене второго уровня *«hotmail»***.

Т.е. нужно отобрать только те записи, что отвечают условию:
- **после символа «@» следует «hotmail»;**
- **после «hotmail» следует символ «.»** и далее любая последовательность символов.

Для таких нетривиальных поисков по строковым полям и нужен оператор **` LIKE `**.

# Синтаксис **` LIKE `**

```PostgreSQL 17.5
... WHERE поле_таблицы [NOT] LIKE шаблон_строки
```

Шаблон может включать два специальных символа — **` % `** и **` _ `**:
- **` % `** - любая последовательность символов;
- **` _ `** - ровно один символ.

Так наш запрос на поиск пользователей в домене ***«hotmail»*** может выглядеть следующим образом:
```PostgreSQL 17.5
SELECT name, email FROM Users
WHERE email LIKE '%@hotmail.%'
```

## <u>Примеры</u>

1. Сопоставляется любым строкам, начинающимся на ***«text»***
```PostgreSQL 17.5
... WHERE поле_таблицы LIKE 'text%'
```

2. Сопоставляется любым строкам, заканчивающимся на ***«text»***
```PostgreSQL 17.5
... WHERE поле_таблицы LIKE '%text'
```

3. Сопоставляется строкам, имеющим длину **4 символа**, при этом **3 последних обязательно должны быть *«ext»***. Например, слова ***«text»*** и ***«next»***
```PostgreSQL 17.5
... WHERE поле_таблицы LIKE '_ext'
```

4. Сопоставляется строкам, начинающихся на ***«begin»*** и заканчивающихся на ***«end»***
```PostgreSQL 17.5
... WHERE поле_таблицы LIKE 'begin%end'
```

---

**➡︎** В PostgreSQL <font color="#ff0000">шаблоны чувствительны к регистру</font>.
**Для поиска без учета регистра используется оператор** **` ILIKE `**

---
# Экранирование специальных символов

Иногда нужно найти строки, в которых символы **` % `** и **` _ `** являются частью самого текста, а не элементами шаблона. В таких случаях эти символы нужно экранировать.

В шаблонах **` LIKE `** для экранирования по умолчанию используется символ **` \ `**. Например, если нужно получить идентификаторы задач, прогресс которых равен 3%, можно написать так:
```PostgreSQL 17.5
SELECT job_id FROM Jobs
WHERE progress LIKE '3\%';
```

## ESCAPE-символ

Если вместо символа по умолчанию нужно использовать другой символ экранирования, его можно указать явно с помощью **` ESCAPE `**.

Синтаксис с **` ESCAPE `**:
```PostgreSQL 17.5
... WHERE поле_таблицы LIKE 'шаблон_строки' ESCAPE 'символ_экранирования'
```

Тот же запрос можно записать с явным указанием символа экранирования:
```PostgreSQL 17.5
SELECT job_id FROM Jobs
WHERE progress LIKE '3!%' ESCAPE '!';
```
Здесь символ ! выполняет ту же роль, что и \ в предыдущем примере.

---
# Практика

<u>Интерактивное упражнение:</u>
![[Pasted image 20260912192452.png|229]]

<u>Задание 1:</u>
Найдите всех членов семьи с фамилией ***"Quincey"*** и выведите поле **` member_name `**
<u>Решение:</u>
```PostgreSQL 17.5
SELECT member_name FROM FamilyMembers WHERE member_name LIKE '%Quincey'
```
